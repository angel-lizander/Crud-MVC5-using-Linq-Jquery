@model IEnumerable<RentCar_2.Models.Carro>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Matricula)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModeloID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Color)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Matricula)
        </td>
        <td>
            @*Html.DisplayFor(modelItem => item.ModeloID)*@
            @Html.DropDownListFor(model => item.Modelo, (IEnumerable<SelectListItem>)ViewBag.model, new { @disabled = "disabled" })

        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Color)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDCarro }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDCarro }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDCarro })
        </td>
    </tr>
}

</table>
